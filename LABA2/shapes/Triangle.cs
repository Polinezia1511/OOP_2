using System.Drawing;

namespace LABA2
{
    /*class Triangle : Shape
    {
        public override void OnChanged()
        {
            //path.Reset();
            PointF[] points = new PointF[3];
            points[0] = firstPoint;
            points[2] = lastPoint;
            points[1] = new PointF(firstPoint.X, lastPoint.Y);
            //path.AddPolygon(points);
        }
       // public override void Draw(Graphics g, Pen pen, Point start, Point finish)
        //{
          //  g.DrawPolygon(pen, start.X, start.Y, finish.X, finish.Y);
        //}
    }
}
*/
}